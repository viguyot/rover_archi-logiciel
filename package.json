{
  "name": "mars-rover-project",
  "version": "1.0.0",
  "description": "Projet Mars Rover Distribué avec architecture DDD",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "clean": "pnpm -r clean",
    "start:rover": "pnpm --filter rover-server start",
    "start:mission": "pnpm --filter mission-control start",
    "dev:rover": "pnpm --filter rover-server dev",
    "dev:mission": "pnpm --filter mission-control dev",
    "test": "pnpm -r test",
    "install:all": "pnpm install"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "rimraf": "^5.0.0",
    "@types/node": "^20.0.0"
  },
  "packageManager": "pnpm@8.0.0"
}
